apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "py-helm.fullname" . }}-test-connection"
  labels:
    {{- include "py-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "py-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
